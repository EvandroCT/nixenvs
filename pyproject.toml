[project]
name = "Mission1"
version = "0.1.0"
description = "Mission 1 environment."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "earthaccess",
    "geopandas",
    "xarray",
    "zarr",
]
