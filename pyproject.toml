[project]
name = "mission3-env"
version = "0.1.0"
description = "Mission 3 environment: GeoZarr datacube assembly from HLS band TIFs."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "xarray",
    "zarr",
    "rioxarray",
    "dask",
    "numpy",
    "pandas",
    "geozarr-toolkit",
]
